var SpanningTree = function (root, tree) {
	this.root = root;
	this.tree = tree; //Array that holds the parent of each vertex (vertices are defined by the index on the array)
};

module.exports = SpanningTree;